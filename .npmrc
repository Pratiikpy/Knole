# Privy's react-auth declares a peerOptional on permissionless→ox@0.8, which
# conflicts with ox@0.14 (pulled via server-auth → viem). permissionless is a
# smart-wallet feature we don't use, so it's skipped; this keeps installs clean.
legacy-peer-deps=true
