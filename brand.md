# Brand — Knole

_Status: deferred_

Knole has a mature design system defined in `src/styles.css` (the source of truth): a warm
"paper & ink" palette (`--paper`, `--ink`, `--tan`), Instrument Serif display + Inter UI, a "Night"
dark theme, and a signature motion language (`--ease-signature`, duration scale, stagger). Use those
tokens — do not run brand-design or introduce a new palette. This file exists only to stop the
frontend-design-guidelines skill from re-prompting.
